enum LoginProgressStatus
{
  loading,
  loginSuccess,
  loginFailed,
  networkIssue
}

enum DataProgressStatus
{
  loading,
  success,
  failed,
  networkIssue
}

enum BookList {
  allBookList,
  filteredBookList,
  searchedBookList
}

enum PendingRequestList {
  allRequestList,
  filteredRequestList,
  searchedRequestList
}